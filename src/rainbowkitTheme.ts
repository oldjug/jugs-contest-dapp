import { darkTheme } from '@rainbow-me/rainbowkit';

export const customDarkTheme = darkTheme({
  accentColor: '#f97316',        // Orange glow
  accentColorForeground: '#000', // Text inside buttons
  borderRadius: 'medium',
  fontStack: 'system',
  overlayBlur: 'small',
});
